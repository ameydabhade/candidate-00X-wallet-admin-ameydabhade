import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  env: {
    NEXT_PUBLIC_API_URL: 'https://wallet-fraud-backend-ameydabhade-ameydabhades-projects.vercel.app'
  }
};

export default nextConfig;
